{
  "name": "ai-job-processor",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd backend && bun run dev\" \"cd frontend && npm run dev\"",
    "build": "concurrently \"cd backend && bun run build\" \"cd frontend && npm run build\"",
    "start": "concurrently \"cd backend && bun run start\" \"cd frontend && npm run start\"",
    "prettier": "prettier . --write",
    "prettier:check": "prettier --check .",
    "prepare": "husky install"
  },
  "devDependencies": {
    "@eslint/js": "^9.11.1",
    "eslint": "^9.11.1",
    "eslint-plugin-react-hooks": "^5.1.0-rc.0",
    "eslint-plugin-react-refresh": "^0.4.12",
    "prettier": "3.5.3",
    "concurrently": "^8.2.1",
    "husky": "^8.0.0"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "lint-staged": {
    "**/*.{js,ts,jsx,tsx,json,md}": "prettier --write"
  }
}
